global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: trading-bot
    static_configs:
      - targets:
          - trading-bot:9464

